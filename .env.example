# Copy this file to `.env` and fill in real values.
# `.env` is gitignored — never commit secrets.

DATABASE_URL="file:./dev.db"
JWT_SECRET="generate-a-long-random-string-here"
NEXT_PUBLIC_APP_NAME="যাত্রা"
